# EXPERIENCES.md — Android 逆向问题经验库（跨 type 共享）

> 本文件记录 Android APK 逆向、改造、重构建和真机验证中可跨项目复用的问题经验。它不是项目日志：不得记录来源 APK、项目名称、包名、设备序列号、日期、单个项目的 RVA 或过程产物路径。
>
> **使用时机**：遇到工具异常、解包失败、分析结果异常、资源或代码修改后失效、构建失败、重签名后闪退、真机启动或功能验证失败时，先检索本文件对应分类，再查当前 type 的 strategy skill。
>
> **分流**：只适用于某个引擎的问题写入该 type skill 的 `experiences.md`；仅属于 SDK 清理领域的问题写入 `skills/common/third-party-removal-strategy-skill/experiences.md`；具体 APK 的事实和过程保留在 `crackings/<type>/<Name>/`。
>
> **条目格式**：所有新增经验必须使用下列字段；修正已有结论时，以新条目追加并标注 `[supersedes: <标题>]`。

```md
### <问题标题>

- **分类**：
- **适用范围**：
- **触发现象**：
- **根因**：
- **解决方案**：
- **验证方式**：
- **注意事项**：
- **相关脚本/文档**：
```

## 快速定位

| 问题表现 | 优先查看 |
|---|---|
| APK/XAPK 无法解包、缺少 native 库或 split | APK/XAPK 解析与资源处理 |
| 引擎识别、DEX/so/SWF/Lua/JSC 分析异常 | 静态分析与引擎解析 |
| D8/R8/AAPT2/CMake/签名错误 | Android Studio 与重构建 |
| 重签名后启动失败、SDK 清理后崩溃 | 运行时、SDK 与完整性校验 |
| 文本、字体、图片替换后显示异常或闪退 | 汉化与资源替换 |
| 安装、启动、截图、日志判定异常 | 真机验证与诊断 |

## 环境与工具链

### 项目内工具链未加载导致的误判

- **分类**：环境与工具链
- **适用范围**：所有 type。
- **触发现象**：系统 PATH 中找不到 adb、apktool、jadx、JDK，或工具版本与项目约束不一致。
- **根因**：直接依赖系统安装的同名工具，未加载项目环境，导致路径或版本漂移。
- **解决方案**：每次阶段操作前加载 `tools/environments/env.sh`；在脚本中通过公共环境封装取得工具路径，不直接依赖系统 PATH。
- **验证方式**：确认 ADB、JDK、apktool、jadx、aapt2、apksigner 与 zipalign 均指向项目内配置的路径和版本。
- **注意事项**：工具缺失前先排除环境加载失败；不要用系统新版本 JDK 或 Android SDK 静默替代项目工具链。
- **相关脚本/文档**：`tools/environments/env.sh`；`OBJECTIVES.md` §2.1；`AGENTS.md` §1.2。

### AGP、Gradle 与 JDK 不兼容

- **分类**：环境与工具链
- **适用范围**：Android Studio 工程重构建。
- **触发现象**：Gradle 同步失败、插件要求更高 JDK，或找不到对应 compileSdk/build-tools。
- **根因**：AGP、Gradle wrapper、JDK 与 Android SDK 平台版本未作为一组统一管理。
- **解决方案**：同步校准根 `build.gradle`、wrapper、`JAVA_HOME`、`compileSdk` 和 `buildToolsVersion`；使用工作区规定的 JDK 11、AGP 7.4.2、Gradle 7.5.1、compileSdk 33 与 build-tools 34.0.0。
- **验证方式**：`assembleRelease` 成功，且构建日志显示预期的 JDK、AGP 与 Gradle 版本。
- **注意事项**：升级 AGP 通常同时要求升级 JDK；不要只改其中一项。
- **相关脚本/文档**：`OBJECTIVES.md` §2.1；对应 type 的 preprocess/build 子阶段。

## APK/XAPK 解析与资源处理

### Split APK 未合并导致 native 库或资源缺失

- **分类**：APK/XAPK 解析
- **适用范围**：XAPK、APKS 与 bundle 安装包。
- **触发现象**：解包后的 base APK 不含目标 ABI 的 `.so`、资源不完整，或安装报 `INSTALL_FAILED_MISSING_SPLIT`。
- **根因**：base split 与 ABI、density、language split 被当作独立 APK 处理。
- **解决方案**：在静态分析和工程化前，将必要 split 合并为 fat APK；同时清理 manifest 中已不匹配的 split 约束。
- **验证方式**：合并产物包含目标 ABI 的 native 库、完整资源表和所有启动所需 DEX；可安装并启动。
- **注意事项**：语言限定资源与资源表容易在不完整合并中损坏；保留原始安装包，只在工作目录处理合并结果。
- **相关脚本/文档**：通用 XAPK merge 子阶段；`STRATEGY.md` §1。

### APKTool 与 AGP 对非标准资源的兼容差异

- **分类**：资源处理
- **适用范围**：资源混淆、游戏引擎导出的资源、旧版 Android 资源表。
- **触发现象**：`apktool b` 或 `mergeReleaseResources` 报资源名非法、raw 值无效、`public.xml` 不一致或 AAPT2 栈溢出。
- **根因**：源 APK 资源可被原始打包链消费，但不一定满足标准 AGP/AAPT2 的资源命名和 values 语义。
- **解决方案**：先定位具体资源；删除无效的 apktool 中间 values 项，修正非法文件名并同步引用，保留或生成稳定资源 ID；若只在 AGP 路径失败，使用 APKTool 路径验证资源问题并修复工程输入。
- **验证方式**：资源编译阶段无错误；构建 APK 中资源可被运行时代码解析。
- **注意事项**：不要批量删除混淆资源；对 `public.xml` 和 hard-coded 资源 ID 的修改必须成对验证。
- **相关脚本/文档**：资源预处理子阶段；`generate-stable-ids.py`；`rename-dollar-res.py`。

### 多 DEX 只被部分反编译或转换

- **分类**：DEX 处理
- **适用范围**：MultiDex APK。
- **触发现象**：jadx 仅显示主 DEX，或生成的 class jar 缺失应用类、启动类和第三方依赖。
- **根因**：工具默认只处理主 DEX，或错误地将多个 DEX 合并后触发类型数量限制。
- **解决方案**：对每个 `classes*.dex` 独立分析与转换；按 DEX 维度保留输入、输出和完整性统计。
- **验证方式**：DEX 数量、关键包和启动类在分析输出与构建输入中均可找到。
- **注意事项**：不要以单个 jar 的成功生成为“所有 DEX 已处理”的依据。
- **相关脚本/文档**：静态分析子阶段；`convert-dex-to-jar.py`；对应 type 的 DEX 转换脚本。

## 静态分析与引擎解析

### 引擎特征冲突或未知引擎被错误路由

- **分类**：类型识别
- **适用范围**：含多个 native 库、混合引擎或自定义运行时的 APK。
- **触发现象**：策略被路由到普通 Android，后续分析工具无法解析核心资产或 native 代码。
- **根因**：仅依据 DEX 存在与否判断类型，忽略 assets、native 库和专属文件格式的组合特征。
- **解决方案**：以策略配置中的 sniff 优先级为准，联合检查 `lib/` 与 `assets/`；发现未覆盖引擎时，新建一对一 strategy skill，而非降级为 Android 兜底。
- **验证方式**：sniff 结果能解释核心运行库和资产格式，后续子阶段使用匹配的工具链。
- **注意事项**：一个 APK 可带多个 SDK native 库；不要把 SDK 库误认为游戏引擎。
- **相关脚本/文档**：`STRATEGY.md` §1、§4；策略配置与 sniff 子阶段。

### 加固或运行时解密导致静态代码不完整

- **分类**：加固与动态分析
- **适用范围**：加壳、DEX 加密、IL2CPP metadata 缺失、字符串加密。
- **触发现象**：smali 不完整、dump 只有空方法、关键类无法反编译，或运行时才出现真实代码。
- **根因**：核心 DEX、metadata 或字符串在启动后解密、映射或由自定义虚拟机解释。
- **解决方案**：先确认加固特征，再使用对应的脱壳、内存 dump 或运行时采集方案；将采集结果回灌到标准静态分析流程。
- **验证方式**：获得可解析的 DEX/metadata，且关键入口、类和字符串能够被交叉验证。
- **注意事项**：不要把加固后 stub DEX 的分析结论当作真实逻辑；动态采集前先确认合法授权与测试范围。
- **相关脚本/文档**：各 type strategy skill 的反调试与动态分析章节。

### 加密脚本或引擎资产直接回写失效

- **分类**：引擎资产解析
- **适用范围**：Cocos Creator、Cocos2d-x、Adobe AIR 及使用专属资产格式的引擎。
- **触发现象**：解密后的脚本可读，但重加密或直接回写后游戏无法加载、静默退出或报资源缺失。
- **根因**：运行时依赖特定加密参数、压缩方式、索引或资源包校验；文本层面的 round-trip 并不等同于引擎运行时兼容。
- **解决方案**：先建立“原始资产可加载”的基线；修改后保留格式、加密参数、压缩与索引，并以启动和目标场景加载做验证。
- **验证方式**：修改后的资产被引擎加载，主菜单和目标场景均可进入。
- **注意事项**：可选绑定脚本与核心启动脚本的风险不同；不确定时优先做小范围替换。
- **相关脚本/文档**：Cocos、AIR 对应 strategy skill 的资产处理章节。

## Android Studio 与重构建

### 标准 AGP 不会编译 smali 源集

- **分类**：工程化与 DEX
- **适用范围**：由 APKTool/FakerAndroid 产物迁移至标准 Android Gradle Plugin 的工程。
- **触发现象**：`assembleRelease` 成功，但安装后 `ClassNotFoundException`、Provider 或 Application 缺失；最终 APK 只有注入骨架类。
- **根因**：标准 AGP 不把 `src/main/smali*` 当作 DEX 源；原始工具的私有 Gradle 插件机制未被带入新工程。
- **解决方案**：将原 APK 的每个 DEX 转为可被 AGP 消费的输入，或在受控的后处理步骤中注入 DEX；保证 manifest 引用的 Application、Activity、Provider 与运行时类位于最终 APK。
- **验证方式**：比较重构建 APK 的 DEX 数量和关键类；安装后可完成 Application 与 Launcher Activity 启动。
- **注意事项**：仅加 keep 规则不能解决“类根本没有进入构建输入”的问题。
- **相关脚本/文档**：`convert-dex-to-jar.py`；`inject-smali-dex.py`；AS 构建子阶段。

### dex2jar 产物被 D8/R8 拒绝或运行时校验失败

- **分类**：D8/R8 与字节码
- **适用范围**：DEX 转 class jar 后由 AGP 重新 dex 化。
- **触发现象**：`Expected stack map table`、desugar 失败、`StackOverflowError`、`VerifyError` 或 lambda 类找不到。
- **根因**：DEX 与 JVM 字节码模型不同；转换器可能丢失 StackMapTable、生成非法 lambda 名称或保留不一致的类型描述符。
- **解决方案**：以 ASM 重算 frames，并一致性重写 lambda 类定义及所有字段、方法、描述符引用；对无法安全转换的超大或异常 DEX，改用 DEX 注入方案。
- **验证方式**：D8/R8 不再报 frame/类型错误；目标 APK 可在设备上加载相关类。
- **注意事项**：字节级字符串替换会破坏 class 引用图；必须用字节码工具处理类型重命名。
- **相关脚本/文档**：`fix-dex-jar-frames.py`；`fix-dex-new-instance.py`；DEX 转换脚本。

### 重复类、R 类或 BuildConfig 引发 D8 递归错误

- **分类**：D8/R8 与依赖
- **适用范围**：将源 APK 类转换为 jar 并同时由 AGP 生成应用类的工程。
- **触发现象**：`checkDuplicateClasses`、`mergeDexRelease` 或 desugar 阶段失败，可能表现为 `StackOverflowError`。
- **根因**：转换 jar 含工程自身的 `R`、`R$*`、`BuildConfig` 或已由依赖提供的类，造成重复定义。
- **解决方案**：在 jar 化前按精确类名剔除工程自动生成类与确定重复的依赖类；保留真正的启动类、引擎桥接类和业务类。
- **验证方式**：重复类报告为空，D8 通过，启动类仍可加载。
- **注意事项**：不要使用宽泛通配符删除类，避免误删名称相近的业务类。
- **相关脚本/文档**：DEX jarify 脚本；`OBJECTIVES.md` 构建约束。

### AGP 打包与源 APK META-INF 条目冲突

- **分类**：APK 打包
- **适用范围**：源 APK 资源或 jar 被直接带入 Android Studio 工程。
- **触发现象**：`packageRelease` 报同一 META-INF entry 已存在，尤其与 Android Gradle Plugin 生成的元数据冲突。
- **根因**：源 APK 含旧构建链元数据，而 AGP 会生成同名条目。
- **解决方案**：仅清理已确认会被 AGP 重新生成的构建元数据；保留服务描述符、Kotlin metadata 与依赖库运行时需要的条目。
- **验证方式**：打包阶段不再报覆盖错误，运行时依赖正常加载。
- **注意事项**：不要把全部 META-INF 一律删除；这会破坏 ServiceLoader、签名或库元数据。
- **相关脚本/文档**：ASBuilder/预处理子阶段；工程化策略文档。

### 资源 ID 重排导致运行时 ResourcesNotFound

- **分类**：资源与重构建
- **适用范围**：源 DEX 或 native 代码硬编码资源 ID 的工程。
- **触发现象**：构建成功，运行时出现 `Resources$NotFoundException` 或 SDK 初始化失败。
- **根因**：AGP 重建 `resources.arsc` 后资源 ID 变化，而原始字节码仍引用旧 ID。
- **解决方案**：从源 `public.xml` 或资源表生成 stable IDs，并让 AAPT2 在构建中使用；无法保真的资源必须同时修复引用方。
- **验证方式**：关键资源 ID 在构建前后保持一致，启动路径不再出现资源找不到。
- **注意事项**：资源删除、重命名和 stable IDs 必须一起审查。
- **相关脚本/文档**：`generate-stable-ids.py`；资源构建子阶段。

## Native、JNI 与 Hook

### CMake 未链接 Hook 实现或引入了空桩

- **分类**：Native 构建
- **适用范围**：含 JNI、inline hook 或预编译 native 库的工程。
- **触发现象**：链接时报未定义符号，或构建成功但 Hook API 始终返回失败、从不生效。
- **根因**：CMake 只收集当前目录源码，未包含子目录实现；同名 stub 提前满足符号，真实库未被链接。
- **解决方案**：显式纳入 Hook 子目录源文件或导入预编译库；删除或隔离与真实实现同名的 stub；在运行时输出加载和安装结果。
- **验证方式**：native 库链接成功，日志显示目标库已加载、基址有效且每个 Hook 安装成功。
- **注意事项**：不要用“编译通过”作为 Hook 有效性的依据。
- **相关脚本/文档**：对应 type 的 native template、CMake 修复脚本与 hook plan。

### Hook 时机、页权限或线程附着导致 native 崩溃

- **分类**：Native 运行时
- **适用范围**：IL2CPP 与其他运行时加载 native 库后才可 Hook 的引擎。
- **触发现象**：目标库基址为零、安装 Hook 时 `SIGSEGV`/`SEGV_ACCERR`，或从 native 回调 Java 时崩溃。
- **根因**：目标库尚未加载、代码页不可写，或当前 native 线程未附着到 JVM。
- **解决方案**：等待目标模块出现后再安装；按架构和平台要求临时调整目标页权限；在 JNI 回调中处理 `JNI_EDETACHED` 的线程附着与释放。
- **验证方式**：日志显示有效基址、Hook 安装成功，目标操作触发后无 native crash。
- **注意事项**：每个 Hook 都应保留可独立禁用的开关，便于二分定位崩溃点。
- **相关脚本/文档**：对应 type 的 native hook 模板；真机验证子阶段。

### Native JNI glue 仍依赖被移除的 Java 类

- **分类**：JNI 与 SDK 清理
- **适用范围**：使用 JNI 回调 Java SDK 或引擎 glue 的 APK。
- **触发现象**：删除 SDK smali 或 `.so` 后发生 `NoClassDefFoundError`、`DllNotFoundException`、JNI abort 或加载页卡死。
- **根因**：native 库在启动或功能调用中仍查找 SDK Java 类、方法或 native 符号。
- **解决方案**：先枚举 JNI 导出和调用链；按依赖情况选择保留最小兼容 stub、清理自动初始化组件，或在已授权的测试范围内对调用入口做安全短路。
- **验证方式**：冷启动、目标功能入口和离线场景均无 JNI 异常。
- **注意事项**：物理删除前必须确认 native 依赖；manifest 清理本身不能移除 native 调用。
- **相关脚本/文档**：第三方 SDK 移除 strategy skill；native 库分析与验证子阶段。

## SDK、网络与完整性校验

### SDK 移除后残留初始化链导致崩溃

- **分类**：第三方 SDK 清理
- **适用范围**：广告、分析、推送、登录、远程配置等第三方 SDK。
- **触发现象**：启动时报 Provider/Service 类缺失、空指针，或加载页无响应。
- **根因**：SDK 的 manifest 组件、Java 初始化、native 库与业务回调只被部分删除。
- **解决方案**：先建立 SDK 依赖清单；按“组件、类、native 库、调用点、资源”完整审查。对仍有业务调用但不应产生副作用的接口使用最小兼容 stub，而非盲目物理删除。
- **验证方式**：manifest 无不需要的自动初始化组件；启动、离线与目标玩法路径无类缺失、JNI 错误或阻塞。
- **注意事项**：保留核心玩法必需的引擎库、支付桥接或兼容依赖；每次删除后立即重构建和真机验证。
- **相关脚本/文档**：`skills/common/third-party-removal-strategy-skill/strategy.md`；SDK 移除与设备验证子阶段。

### 强制联网检测或远程配置阻断单机流程

- **分类**：网络适配
- **适用范围**：含启动联网检查、版本检查、远程配置或奖励回调的单机游戏。
- **触发现象**：飞行模式下提示网络错误、卡在加载页，或奖励路径因请求失败不能完成。
- **根因**：网络状态、远程响应与核心状态机强耦合。
- **解决方案**：定位实际阻断点，保持接口返回类型和状态机预期一致；让本地路径提供成功、失败和超时的可控结果，而非只修改一个网络 API 返回值。
- **验证方式**：在无网络环境可冷启动、进入主菜单和核心玩法；不产生持续重试或 ANR。
- **注意事项**：保留系统网络权限不等于保留强制网络依赖；不要把广告或云功能的可选失败误判为核心阻断。
- **相关脚本/文档**：网络检测移除子阶段；对应 type 的 strategy skill。

### 重签名触发完整性、许可证或反调试保护

- **分类**：完整性校验与加固
- **适用范围**：含签名绑定、安装来源校验、许可证或反调试机制的 APK。
- **触发现象**：未修改功能仍在重签名后立即闪退、退出或出现确定性的 native 崩溃。
- **根因**：运行时校验签名证书、安装器、服务状态或调试环境，且校验可能在 native 层完成。
- **解决方案**：先识别保护类型和触发点；记录其为重构建阻塞条件，并使用与授权范围一致的测试策略。无法安全重签名的保护不得伪报为已完成。
- **验证方式**：原始与测试产物的启动行为可复现区分；日志或调用链能定位校验触发点。
- **注意事项**：不要将规避第三方完整性保护当作通用默认步骤；优先记录限制、影响和合法替代测试方式。
- **相关脚本/文档**：反调试/完整性策略章节；真机诊断子阶段。

## 汉化与资源替换

### 文本替换未覆盖游戏 UI 或造成字符串损坏

- **分类**：文本汉化
- **适用范围**：资源字符串、IL2CPP metadata、脚本和运行时 UI 文本。
- **触发现象**：`strings.xml` 已替换但游戏 UI 仍为原语言，或替换后占位符、编码和布局异常。
- **根因**：游戏文本可能位于 native metadata、加密脚本、场景资产或运行时生成逻辑，而非 Android 资源。
- **解决方案**：先定位文本来源；为不同存储层采用对应替换方案；保留格式化占位符、控制字符和原始编码，并使用小批量映射验证。
- **验证方式**：目标场景 OCR 或人工检查文本显示正确，无乱码和格式化异常。
- **注意事项**：避免模糊关键字整串替换；包含上下文的文本须人工确认语义。
- **相关脚本/文档**：文本汉化子阶段；各引擎 localization strategy。

### 字体、图片或场景资产替换后崩溃

- **分类**：字体与图片汉化
- **适用范围**：Android resources、Unity/Cocos 场景资产和纹理。
- **触发现象**：AAPT2 拒绝文件名或格式，运行时字体加载失败、纹理异常、场景崩溃或显示错位。
- **根因**：替换文件不符合资源命名规则、字体格式不兼容，或图片尺寸、色彩格式、9-patch 边界和资产包索引发生变化。
- **解决方案**：使用合法的小写资源名；保持原文件格式、尺寸和关键元数据；对引擎资产使用其专属编辑工具并做单资源回归。
- **验证方式**：构建通过，目标页面文字可显示、图片无拉伸或透明异常，设备日志无字体/纹理加载错误。
- **注意事项**：不要将 Android `res/` 命名规则直接套到引擎 assets；替换前保存原始文件的尺寸和校验信息。
- **相关脚本/文档**：字体、文本与图片汉化子阶段；对应 type 的 assets 工具。

## 真机验证与诊断

### 真机验收脚本把其他应用错误误判为当前应用崩溃

- **分类**：设备验证
- **适用范围**：共享测试设备、历史 logcat 未清理的环境。
- **触发现象**：验证报告发现 FATAL/SIGSEGV，但目标进程仍存活且页面可用。
- **根因**：直接扫描全局日志，将其他应用或历史 tombstone 纳入当前应用结果。
- **解决方案**：以目标包名、PID、启动时间窗和目标 Activity 过滤日志；将安装、启动、存活、截图与错误日志作为一份同批次证据输出。
- **验证方式**：报告中的错误行可关联到目标进程；其他应用错误不会改变当前应用判定。
- **注意事项**：进程存活不等于核心玩法可用，仍需执行阶段定义的 UI/功能验证。
- **相关脚本/文档**：`device_verify_common.py`；各 device-verify 子阶段。

### 安装成功但启动或加载阶段失败

- **分类**：运行时诊断
- **适用范围**：所有重构建或重打包 APK。
- **触发现象**：安装无误，但启动后黑屏、闪退、卡加载或 ANR。
- **根因**：构建阶段未能证明运行时完整性；常见原因包括缺 DEX、缺 `.so`、manifest 指向失效、资源 ID 改变、SDK/JNI 残留依赖或完整性校验。
- **解决方案**：按“进程/Activity → Java 异常 → native 崩溃 → 资源与网络状态机”的顺序二分；每次只恢复或禁用一个变更，形成可重现的最小差异。
- **验证方式**：冷启动、重启、离线启动及目标功能入口均通过；日志无当前应用的 FATAL/ANR/native crash。
- **注意事项**：不要因为二次启动偶然成功而宣告修复；必须验证冷启动和目标场景。
- **相关脚本/文档**：真机验证子阶段；`WORKFLOW.md` 的阶段验收要求。

## 维护规则

- 仅记录可跨项目复用的逆向问题、根因、处理边界和验证方法。
- 项目事实、APK 文件名、包名、日期、设备信息、RVA、临时命令和项目过程记录保留在 `crackings/<type>/<Name>/` 的状态与发现文档中，不写入本文件。
- 引擎专属的流程、工具选择和资产格式说明以对应 `skills/strategy/<type>-strategy-skill/` 为准；本文件只保留其跨项目问题结论。
- 新发现的可复用操作应优先固化为正式脚本并登记脚本索引；本文件引用脚本，不存放一次性命令。
